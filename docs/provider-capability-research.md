# Provider capability research (26 September 2026)

This note turns current provider and platform documentation into *candidate* Aistee work. The maintained product backlog is [product-ideas.md](product-ideas.md); existing transport and state rules are in [provider-runtime.md](provider-runtime.md). It does not claim that an account-backed WebView exposes an API feature or that API access is free. Status was checked against `main` at `77b5d0b`; the overlapping APK research, Save to Library share target and OpenAI background jobs are already in PRs #286, #287 and #285.

## Recommended order

| Priority | Candidate and smallest useful slice | Boundary / acceptance check | Evidence |
| --- | --- | --- | --- |
| 1 | Finish **Copilot and Grok Web verification** in the existing provider matrix: one manual sign-in/upload/generation-state checklist per provider and method; record the WebView package and result without credentials or page content. For reliable provider-owned pages, consider AndroidX WebKit document-start scripts scoped by exact allowed origins and runtime feature detection before trying a faster activity probe. | Keep the current static, provider-scoped tweaks and no JavaScript-to-native bridge. Do not promise account-history or API abilities from WebView observations. Unsupported embedded sign-in remains an explicit browser/manual path. | [AndroidX WebKit API](https://github.com/androidx/androidx/blob/androidx-main/webkit/webkit/api/current.txt), [existing WebView status](../README.md#provider-support-matrix) |
| 2 | Add **input budget preflight** for selected native/API chats and Token Arena. Start with an explicit action that counts the *actual assembled request* (instructions, tool definitions, selected project context, bounded history and attachments) via Gemini `countTokens` or Claude `messages/count_tokens`; show model, source of count and estimated remaining room. | Network-backed counts require a deliberate action because they transmit the request. Keep the offline `o200k_base` reference labeled as an encoding-specific baseline, never a Gemini/Claude exact count. Do not send a separate counting request automatically on every keystroke. | [Gemini tokens](https://ai.google.dev/gemini-api/docs/tokens), [Claude token counting](https://platform.claude.com/docs/en/build-with-claude/token-counting) |
| 3 | Build **source-linked answers from selected Project Library assets**: explicitly select a file, show the outgoing excerpt/file, keep source IDs in local message metadata and let a cited result jump to the source. Start with bounded local excerpts and line/section references; evaluate provider-hosted retrieval only for large collections. | A local asset stays local until the user chooses an API destination. Distinguish local excerpt references from provider-returned citations. If using hosted retrieval, show storage/deletion controls and costs before upload; deletion of Aistee local data must not be presented as deletion of provider copies. Claude citations and strict structured outputs cannot be used together in one request. | [Claude citations](https://platform.claude.com/docs/en/build-with-claude/citations), [OpenAI file search](https://developers.openai.com/api/docs/guides/tools-file-search), [OpenAI data controls](https://developers.openai.com/api/docs/guides/your-data), [Gemini Files](https://ai.google.dev/gemini-api/docs/files), [xAI file retention](https://docs.x.ai/developers/rest-api-reference/files/upload) |
| 4 | Give **Docbench-style transformations a typed result**: define a small shared contract such as `kind`, `content`, `warnings` and `sourceIds`; use provider-supported schema output for JSON/YAML extraction or repair, then validate the returned document locally and let the user preview/save it in Project Library. | Keep format-specific request mapping behind direct-provider adapters; a JSON-mode promise is weaker than schema adherence. WebView chats get a user-reviewed copy/share path. For Claude, use separate citation and strict-JSON workflows because the combination is rejected. | [OpenAI Structured Outputs](https://developers.openai.com/api/docs/guides/structured-outputs), [Gemini structured output](https://ai.google.dev/gemini-api/docs/structured-output), [Claude structured outputs](https://platform.claude.com/docs/en/build-with-claude/structured-outputs), [xAI structured outputs](https://docs.x.ai/developers/model-capabilities/text/structured-outputs) |
| 5 | Make **tool permission decisions visible** in native chat: a compact per-call receipt with tool name, selected input scope, allowed/denied result and destination of any resulting artifact. Reuse the existing `ALLOW / DENY / ASK / REQUIRES_USER_INTERACTION` registry and bounded loops. | A model request never grants file/network access. Tool output remains untrusted; consent for an action with side effects cannot be inferred from enabling a skill. Claude Code's per-tool interaction flag is a design reference, not a portable Aistee wire contract. | [Claude Code MCP approvals](https://code.claude.com/docs/en/mcp#require-approval-for-a-specific-tool), [Claude Code security](https://code.claude.com/docs/en/security), [xAI function calling](https://docs.x.ai/developers/tools/function-calling) |

### Provider-specific decisions

- **Microsoft Copilot:** keep the existing consumer/account Copilot WebView. Microsoft 365 Copilot Chat API is a *different*, preview, Graph-based enterprise integration requiring a Microsoft 365 Copilot add-on license per user. It offers text and SSE, with no file creation or long-running tool jobs in the documented API. Do not add it to the free/native provider picker or reuse consumer WebView credentials. Revisit only if an actual licensed organizational use case appears. [Microsoft Learn: Chat API overview](https://learn.microsoft.com/microsoft-365/copilot/extensibility/api/ai-services/chat/overview), [Copilot APIs overview](https://learn.microsoft.com/microsoft-365/copilot/extensibility/copilot-apis-overview).
- **Grok:** keep Grok Web and a possible xAI native/API adapter separate. xAI documents function calls, structured outputs and files, but an OpenAI-compatible base URL is not proof that every Responses tool or free route behaves identically. Test exact model/tool behavior and key/budget limits before adding a direct adapter. xAI files remain until deletion unless an expiry was set at upload; any future upload flow needs provider file IDs and explicit cleanup. [xAI function calling](https://docs.x.ai/developers/tools/function-calling), [xAI file lifecycle](https://docs.x.ai/developers/files/managing-files).
- **OpenAI, Claude, Gemini:** #285 already implements OpenAI Background jobs; extend its existing job store if another provider gets a real async API. For repeated large project instructions, benchmark provider-specific caching only after measuring tokens and latency; avoid a cross-provider "cache on" switch with invented savings. [OpenAI background](https://developers.openai.com/api/docs/guides/background), [Claude prompt caching](https://platform.claude.com/docs/en/build-with-claude/prompt-caching), [Gemini context caching](https://ai.google.dev/gemini-api/docs/caching).

## Before implementation

First close the existing WebView verification item. Then make each native slice its own PR: (a) deliberate token preflight, (b) selected-library context and source provenance, (c) typed Docbench export, (d) tool-call receipts. Verify current model support, cost, retention and AndroidX feature availability at implementation time. No new account, hosted backend, remote skill execution or duplicate `.ai` configuration is implied by this research.
